{
  "name": "common",
  "version": "0.1.0",
  "private": true,
  "workspaces": {
    "nohoist": [
      "**/@typescript-eslint/eslint-plugin",
      "**/@typescript-eslint/parser",
      "**/babel-core",
      "**/babel-jest",
      "**/babel-loader",
      "**/jest"
    ],
    "packages": [
      "apps/*",
      "packages/*"
    ]
  },
  "scripts": {
    "lint": "yarn workspace lib-common run build; yarn workspace lib-tests run build; yarn workspaces run lint",
    "test": "yarn workspace lib-common run build; yarn workspace lib-tests run build; yarn workspaces run test",
    "build-tests": "yarn workspace lib-common run build; yarn workspace lib-tests run build",
    "build-lti": "yarn workspace lib-common run build; yarn workspace lib-components run build; yarn workspace marsha run build",
    "prettier": "yarn workspaces run prettier"
  },
  "resolutions": {
    "@codemirror/view": "6.2.4",
    "@codemirror/state": "6.1.1",
    "@types/mdast": "3.0.10",
    "@types/react": "17.0.43",
    "@types/react-dom": "17.0.14",
    "grommet": "2.25.1",
    "unist-util-visit": "4.1.1"
  }
}
